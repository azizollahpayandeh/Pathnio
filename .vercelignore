# Shared by both Vercel projects (frontend root=frontend, backend root=backend).
# Do NOT ignore whole app dirs here — the backend project reads this same file
# and ignoring "backend" strips its Django source (no entrypoint found).
.git
node_modules
.next
out
*.rar
*.sqlite3
*.log
.env*
**/__pycache__
