# Copy to .env.local (gitignored). Both are optional locally - src/app/api.ts
# already falls back to http://localhost:8000 when running on localhost, and
# to the deployed backend otherwise.

NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
